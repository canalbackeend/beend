export const metadata = {
  title: 'Login do Terminal - Pesquisa de Satisfação',
  description: 'Acesse o painel de visualização do terminal',
};

export default function TerminalLoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
